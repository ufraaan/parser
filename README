parser
=====

tiny go library that fetches a webpage and pulls out the title, meta
description, links, and body text. also tokenises text into words.

normalises urls, strips scripts and styles, filters out mailto/tel/etc
links, and only keeps words longer than 2 chars when tokenising.

uses goquery (https://github.com/PuerkitoBio/goquery)